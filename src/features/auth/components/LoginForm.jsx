import styles from './LoginForm.module.css'

export const LoginForm = () => {
  return (
    <div className="flex justify-center items-center">
      <h3 className={`flex w-full justify-center ${styles.formTitle}`}>Login Form</h3>
    </div>
  )
}
